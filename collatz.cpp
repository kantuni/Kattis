// 1. Build a binary tree
// 2. Find LCA for each query

#include <bits/stdc++.h>
using namespace std;

struct Node {
  unsigned long long value;
  Node *left, *right;
};

int main() {
  int a, b;
  while (cin >> a >> b) {
    if (a == 0 and b == 0) {
      break;
    }
  }
  return 0;
}
